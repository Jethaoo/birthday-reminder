import DesignSurface from './DesignSurface'

/**
 * The design surface for Birthday Reminder V1.
 *
 * This app is the reference for the visual language only — the shipping product
 * is the Flutter Android app in `app/`, backed by the Worker in `backend/`.
 */
export default function App() {
  return <DesignSurface />
}
